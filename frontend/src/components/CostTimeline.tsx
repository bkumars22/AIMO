/**
 * CostTimeline — 7-day cost-per-pipeline area chart (Recharts).
 * Phase 1: fetch /ai/cost/timeline and render.
 */
export default function CostTimeline() {
  return (
    <section className="bg-gray-900 border border-gray-800 rounded-lg p-4">
      <h2 className="text-sm font-semibold text-gray-200 mb-3">Cost Timeline (7d)</h2>
      <div className="flex items-center justify-center h-40 text-gray-600 text-xs">
        Phase 1 — Recharts area chart coming soon
      </div>
    </section>
  )
}
