# ── Web framework ─────────────────────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.30.1

# ── LangGraph / LangChain ─────────────────────────────────────
langgraph>=0.1.19
langchain>=0.2.6
langchain-groq>=0.1.6
langchain-anthropic>=0.1.19
langsmith>=0.1.82

# ── LLM clients ───────────────────────────────────────────────
anthropic>=0.30.0
groq>=0.9.0

# ── AI Evaluation ─────────────────────────────────────────────
deepeval>=0.21.44

# ── ML (IsolationForest anomaly detection) ────────────────────
scikit-learn>=1.5.0
numpy>=1.26.4

# ── Embeddings (sentence-transformers all-MiniLM-L6-v2) ───────
sentence-transformers>=3.0.1

# ── Database ──────────────────────────────────────────────────
psycopg2-binary>=2.9.9
pgvector>=0.3.0
sqlalchemy>=2.0.31
alembic>=1.13.2

# ── Redis ─────────────────────────────────────────────────────
redis>=5.0.7

# ── Scheduling ────────────────────────────────────────────────
apscheduler>=3.10.4

# ── Config / Auth ─────────────────────────────────────────────
pydantic>=2.8.2
pydantic-settings>=2.3.4
python-dotenv>=1.0.1
python-jose[cryptography]>=3.3.0

# ── HTTP client (async span shipping) ─────────────────────────
httpx>=0.27.0

# ── Testing ───────────────────────────────────────────────────
pytest>=8.2.2
pytest-asyncio>=0.23.7
pytest-cov>=5.0.0
